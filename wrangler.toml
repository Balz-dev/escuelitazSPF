#:schema node_modules/wrangler/config-schema.json
name = "escuelitazspf"
compatibility_date = "2024-11-01"
pages_build_output_dir = "out"

# Las variables NEXT_PUBLIC_* deben configurarse en el panel de Cloudflare Pages:
# Settings → Variables and Secrets → "Add variable" (sección de Production/Preview)
# IMPORTANTE: Para que el build de Next.js las detecte, activa "Available during build time"
#
# Variables requeridas:
#   NEXT_PUBLIC_SUPABASE_URL
#   NEXT_PUBLIC_SUPABASE_ANON_KEY
